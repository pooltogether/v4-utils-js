import { Interface } from "@ethersproject/abi";

import ABI_PrizeTierHistory from "@pooltogether/v4-periphery/abis/PrizeTierHistory.json";

export const InterfacePrizeTierHistory = new Interface(ABI_PrizeTierHistory);
