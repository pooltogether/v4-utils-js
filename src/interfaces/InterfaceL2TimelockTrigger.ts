import { Interface } from "@ethersproject/abi";

import ABI_L2TimelockTrigger from "@pooltogether/v4-timelocks/abis/L2TimelockTrigger.json";

export const InterfaceL2TimelockTrigger = new Interface(ABI_L2TimelockTrigger);
