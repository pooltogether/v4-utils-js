import { Interface } from "@ethersproject/abi";

import ABI_L1TimelockTrigger from "@pooltogether/v4-timelocks/abis/L1TimelockTrigger.json";

export const InterfaceL1TimelockTrigger = new Interface(ABI_L1TimelockTrigger);
